/*
 * 필터 드롭다운 (주문 상태·결제·유형 등)
 *
 * Props 후보: label, value, options[{ value, label }], onChange, disabled?
 * 주문: orderStatus / paymentStatus / orderType 값 집합은 README §2
 */
export default function AdminFilterDropdown() {
  return null;
}
