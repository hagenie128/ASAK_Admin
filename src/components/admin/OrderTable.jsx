/*
 * 주문 목록 표 (SCR-010)
 *
 * mock rows: getAdminOrders().data.content[]
 *   orderId, orderNo, orderType, totalPrice, orderStatus, paymentStatus,
 *   paymentMethod, createdAt, itemCount, menuSummary
 * Props 후보: rows, filters, selectedId, onSelect, listStatus
 * 표: public/mocks/README.md §2 — Live menus 스키마와 다름
 */
export default function OrderTable() {
  return null;
}
