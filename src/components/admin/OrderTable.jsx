/*
 * Figma Component 연결 후보: DataTable (주문 전용 특수화) / 행 셀
 * 현재 코드 역할: SCR-009 주문 목록 테이블 Presentational
 * 최종 명칭 확인 필요: OrderTable vs 공용 DataTable.jsx 와 병합 여부 (Rename 보류)
 * Figma 승인 후 연결할 Props: rows, selectedId, onSelectRow, columns
 * 이 파일이 직접 처리하면 안 되는 상태: fetch, 필터 소유, 상태 PATCH
 *
 * TODO 1: DataTable과 역할이 겹치면 Figma 승인 후 하나만 남긴다
 * TODO 2: 완성 JSX/CSS 금지
 */

export default function OrderTable(/* props */) {
  return null;
}
