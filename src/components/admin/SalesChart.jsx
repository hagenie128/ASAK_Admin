/*
 * Figma Component 연결 후보: (Chart — Figma Master 명칭 확인)
 * 현재 코드 역할: 일별 매출 차트 Presentational
 * 최종 명칭 확인 필요: SalesChart
 * Figma 승인 후 연결할 Props: series (adapter 결과), empty 여부
 * 이 파일이 직접 처리하면 안 되는 상태: 기간 Filter, fetch, Table 변환
 *
 * TODO 1: series는 salesAdapter.toSalesChartSeries 결과만 받는다
 * TODO 2: 차트 라이브러리 추가는 Figma/스택 결정 후 (새 UI 라이브러리 임의 설치 금지)
 */

export default function SalesChart(/* props */) {
  return null;
}
