/*
 * Figma Component 연결 후보: (배지 — Figma Master에서 Status Badge 명칭 확인)
 * 현재 코드 역할: 주문 상태를 목록·상세에서 같은 라벨/스타일 슬롯으로 표시
 * 최종 명칭 확인 필요: OrderStatusBadge
 * Figma 승인 후 연결할 Props: status (RECEIVED|PREPARING|COMPLETED)
 * 이 파일이 직접 처리하면 안 되는 상태: 상태 변경 API, 색상 토큰 임의 창작
 *
 * TODO 1: 목록과 상세가 동일 컴포넌트만 사용
 * TODO 2: 완성 CSS는 Figma 토큰 연결 후
 */

export default function OrderStatusBadge(/* props */) {
  return null;
}
