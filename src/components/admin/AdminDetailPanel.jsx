/*
 * 주문/메뉴 상세 패널
 *
 * 주문 mock: getAdminOrderById(id).data
 *   items[{ menuId, menuName, quantity, unitPrice, optionItems[], excludedIngredients[] }]
 * Props 후보: order | menu, isUpdating, onChangeStatus, onClose
 * 표: public/mocks/README.md §2 · §5
 */
export default function AdminDetailPanel() {
  return null;
}
