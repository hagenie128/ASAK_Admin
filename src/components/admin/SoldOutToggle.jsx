/*
 * Figma Component 연결 후보: (Toggle — Figma Master 명칭 확인)
 * 현재 코드 역할: 메뉴/재료/옵션 품절 토글 Presentational
 * 최종 명칭 확인 필요: SoldOutToggle
 * Figma 승인 후 연결할 Props: checked, disabled, label, onToggle
 * 이 파일이 직접 처리하면 안 되는 상태: Draft 저장, 서버 PATCH, 실패 복구
 *
 * 사건만 상위로: onToggle(nextValue) → useSoldOutDraft
 * TODO 1: 요청 중 disabled
 * TODO 2: 완성 스타일 금지
 */

export default function SoldOutToggle(/* props */) {
  return null;
}
